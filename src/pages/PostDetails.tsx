import { CommentCardsList } from '../components/CommentCardsList';
import { Layout } from '../components/Layout';
export const PostDetails = () => {
  return (
    <Layout>
      <CommentCardsList />
    </Layout>
  );
};
