import { Layout } from '@shared/layouts/Layout';
import { LoginForm } from '@modules/auth';

const Login = () => {
  return (
    <Layout>
      <LoginForm />
    </Layout>
  );
};

export default Login;
