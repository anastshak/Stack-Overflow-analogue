import { Layout } from '@shared/layouts/Layout';
import { UsersList } from '@modules/users';

const Users = () => {
  return (
    <Layout>
      <UsersList />
    </Layout>
  );
};

export default Users;
