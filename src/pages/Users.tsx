import { Layout } from '../components/Layout';
import { UsersCardsList } from '../components/UserCardsList';

export const Users = () => {
  return (
    <Layout>
      <UsersCardsList />
    </Layout>
  );
};
